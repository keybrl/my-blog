<div class="mdl-mini-footer--left-section sns-list">
    <!-- Twitter -->
    <% if(theme.sns.twitter) { %>
        <a href="<%= theme.sns.twitter %>" target="_blank">
            <i class="fa-social-btn fa fa-twitter-square"></i><!--
     --></a>
    <% } %>

    <!-- Facebook -->
    <% if(theme.sns.facebook) { %>
        <a href="<%= theme.sns.facebook %>" target="_blank">
            <i class="fa-social-btn fa fa-facebook-square"></i><!--
     --></a>
    <% } %>

    <!-- Google + -->
    <% if(theme.sns.googleplus) { %>
        <a href="<%= theme.sns.googleplus %>" target="_blank">
            <i class="fa-social-btn fa fa-google-plus-square"></i><!--
     --></a>
    <% } %>

    <!-- Weibo -->
    <% if(theme.sns.weibo) { %>
        <a href="<%= theme.sns.weibo %>" target="_blank">
            <i class="fa-social-btn fa fa-weibo"></i><!--
     --></a>
    <% } %>

    <!-- Instagram -->
    <% if(theme.sns.instagram) { %>
        <a href="<%= theme.sns.instagram %>" target="_blank">
            <i class="fa-social-btn fa fa-instagram"></i><!--
     --></a>
    <% } %>

    <!-- Tumblr -->
    <% if(theme.sns.tumblr) { %>
        <a href="<%= theme.sns.tumblr %>" target="_blank">
            <i class="fa-social-btn fa fa-tumblr-square"></i><!--
     --></a>
    <% } %>

    <!-- Github -->
    <% if(theme.sns.github) { %>
        <a href="<%= theme.sns.github %>" target="_blank">
            <i class="fa-social-btn fa fa-github-square"></i><!--
     --></a>
    <% } %>

    <!-- LinkedIn -->
    <% if(theme.sns.linkedin) { %>
        <a href="<%= theme.sns.linkedin %>" target="_blank">
            <i class="fa-social-btn fa fa-linkedin-square"></i><!--
     --></a>
    <% } %>

    <!-- Telegram -->
    <% if(theme.sns.telegram) { %>
        <a href="<%= theme.sns.telegram %>" target="_blank">
            <i class="fa-social-btn fa fa-telegram"></i><!--
     --></a>
    <% } %>

    <!-- Steam -->
    <% if(theme.sns.steam) { %>
        <a href="<%= theme.sns.steam %>" target="_blank">
            <i class="fa-social-btn fa fa-steam"></i><!--
     --></a>
    <% } %>

    <!-- RSS -->
    <% if(theme.sns.rss) { %>
        <a href="<%= theme.sns.rss %>" target="_blank">
            <i class="fa-social-btn fa fa-rss-square"></i><!--
     --></a>
    <% } %>
</div>
<style>
#scheme-Paradox .fa-social-btn {
    color: #8a8a8a;
    font-size: 24px;
    margin: 0 16px;
}
</style>
